import { StyleSheet } from 'react-native';

import { COLOR_PRIMARY, COLOR_TEXT, FONT_REGULAR, BORDER_RADIUS } from './../styles/common';

export default StyleSheet.create({
  text: {
    color: COLOR_TEXT,
    fontFamily: FONT_REGULAR,
    borderRadius: BORDER_RADIUS,
    padding: 15,
    fontSize: 18,
    flex: 1,

  },
});
